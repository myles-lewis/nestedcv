% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shap.R
\name{nestcv.explain}
\alias{nestcv.explain}
\title{Generate SHAP values from nestedcv models using shapr}
\usage{
nestcv.explain(
  model,
  x_explain,
  predict_model,
  x_train = NULL,
  approach = NULL,
  phi0 = NULL,
  min_obs_per_feature = 20,
  ...
)
}
\arguments{
\item{model}{A \code{nestcv.glmnet}, \code{nestcv.train}, or other \code{nestedcv} model
object.}

\item{x_explain}{A matrix or data frame of feature values to compute SHAP
values for.}

\item{predict_model}{Prediction wrapper function with signature
\verb{function(model, newdata)} returning a numeric vector of predictions.
Use \code{\link[=pred_nestcv_glmnet]{pred_nestcv_glmnet()}}, \code{\link[=pred_train]{pred_train()}}, \code{\link[=pred_nestcv_glmnet_class]{pred_nestcv_glmnet_class()}},
\code{\link[=pred_train_class]{pred_train_class()}}, or \code{\link[=pred_SuperLearner]{pred_SuperLearner()}} as appropriate.}

\item{x_train}{A matrix or data frame of feature values used as the
background training data. Defaults to \code{model$xsub[, model$final_vars]}
when \code{model} stores these (e.g. a fitted \code{nestcv.glmnet}/\code{nestcv.train});
otherwise falls back to \code{x_explain} (matching \code{fastshap} behaviour of
using the same data for both).}

\item{approach}{Character string specifying the shapr estimation approach.
Defaults to \code{NULL}, in which case it is auto-selected from \code{x_train}'s
column types: \code{"categorical"} if all columns are factor/character,
\code{"ctree"} if mixed numeric and factor/character, or for all-numeric
\code{x_train}, \code{"empirical"} if there are at least \code{min_obs_per_feature}
observations per feature, otherwise \code{"independence"}.
Pass any other value accepted by \code{\link[shapr:explain]{shapr::explain()}} (e.g. \code{"vaeac"},
\code{"gaussian"}, \code{"copula"}) to override.}

\item{phi0}{Numeric scalar; the baseline (null) prediction (i.e. the
expected model output when no features are known). Defaults to \code{NULL},
in which case it is automatically computed as
\code{mean(predict_model(model, x_explain))} on the supplied data. For
regression this equals \code{mean(y_train)}; for classification it equals the
mean predicted probability. Override this argument if you want to use a
different reference value, e.g. computed on a held-out set.}

\item{min_obs_per_feature}{Numeric scalar specifying the minimum ratio of
\code{nrow(x_train)} to \code{ncol(x_train)} required to auto-select \code{"empirical"}
for all-numeric \code{x_train} (see \code{approach} above). Defaults to \code{20}.
Ignored if \code{approach} is supplied explicitly.}

\item{...}{Additional arguments passed to \code{\link[shapr:explain]{shapr::explain()}}, e.g.
\code{verbose}.}
}
\value{
the \code{shapr} object returned by \code{\link[shapr:explain]{shapr::explain()}}. Pass it
directly to \code{\link[=plot_shap_bar]{plot_shap_bar()}} or \code{\link[=plot_shap_beeswarm]{plot_shap_beeswarm()}}, or use
\code{shapr}'s own \code{print()}/\code{plot()} methods on it.
}
\description{
Convenience wrapper around \code{\link[shapr:explain]{shapr::explain()}} that works with \code{nestedcv}
fitted models. Returns the \code{shapr} object directly, compatible with
\code{\link[=plot_shap_bar]{plot_shap_bar()}}, \code{\link[=plot_shap_beeswarm]{plot_shap_beeswarm()}}, and \code{shapr}'s own \code{print()}/
\code{plot()} methods.
}
